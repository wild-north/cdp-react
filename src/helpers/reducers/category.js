export const enableEdit = (state, id) => {
    /**
     * TODO: Установить editCategoryId в значение id (id категории)
     */
    // tmpTitle: this.props.item.name,
        // editMode: true
};

export const disableEdit = (state, id) => {
    /**
     * TODO: Установить editCategoryId в значение null
     */
    // this.setState(this.defaultState);
};

export const saveCategoryTitle = (state, { id, name: newName }) => {
    /**
    * TODO: Если текущее имя тайтла категории не совпадает с новым именем, заменить тайтл категории
    */
    // if (item.name !== this.state.tmpTitle) {
    //     rename(id, this.state.tmpTitle);
    // }
    // this.setState(this.defaultState);
};

export const changeTmpTitle = (state, { tmpTitle }) => {
    /**
    * TODO: Установить store.tmpTitle в полученное в аргументе значение
    */
    // if (item.name !== this.state.tmpTitle) {
    //     rename(id, this.state.tmpTitle);
    // }
    // this.setState(this.defaultState);
};